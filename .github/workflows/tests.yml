name: Run tests

on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop

env:
  AWS_DEFAULT_REGION: ap-southeast-2

jobs:
  tests:
    name: tests
    runs-on: ${{ matrix.os }}
    if: "!contains(github.event.head_commit.message, 'nogha')"

    strategy:
      matrix:
        os: ["ubuntu-20.04"]

    steps:
      - uses: actions/checkout@v3

      - name: Cache build image
        uses: actions/cache@v3
        continue-on-error: false
        with:
          path: /home/runner/image-build.tgz
          key: ansilo-test-${{ runner.os }}-${{ hashFiles('.github/workflows/docker/build.ubi9.Dockerfile') }}

      - name: Cache cargo/maven registry
        uses: actions/cache@v3
        continue-on-error: false
        with:
          path: |
            m2.tgz
            cargo.tgz
          key: ansilo-test-pkg-${{ runner.os }}-${{ hashFiles('**/Cargo.lock', '**/pom.xml') }}

      - name: Cache target dir
        uses: actions/cache@v3
        continue-on-error: false
        with:
          path: target.tgz
          key: ansilo-test-target-${{ runner.os }}-${{ github.run_id }}
          restore-keys: | 
            ansilo-test-target-${{ runner.os }}

      - name: Extract caches
        run: |
          .github/workflows/scripts/cache-extract.sh m2.tgz ~/
          .github/workflows/scripts/cache-extract.sh cargo.tgz ~/
          .github/workflows/scripts/cache-extract.sh target.tgz .

      - name: Build base image
        run: .github/workflows/scripts/build-build-image.sh

      - name: Create volume dirs
        run: .github/workflows/scripts/volumes.sh

      - name: Cargo build
        run: |
          docker run \
            -v $HOME/.cargo:/home/build/.cargo \
            -v $HOME/.m2:/home/build/.m2 \
            -v $PWD:/build/ \
            -w /build/ \
            ansilo-build /build/.github/workflows/scripts/test-build.sh

      - name: Test setup
        run: |
          docker run \
            -e AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID \
            -e AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY \
            -v $HOME/.cargo:/home/build/.cargo \
            -v $HOME/.m2:/home/build/.m2 \
            -v $PWD:/build/ \
            -w /build/ \
            ansilo-build /build/.github/workflows/scripts/test-setup.sh
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.TESTS_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TESTS_AWS_SECRET_ACCESS_KEY }}

      # - name: Run tests
      #   run: |
      #     docker run \
      #       -e AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID \
      #       -e AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY \
      #       -v $HOME/.cargo:/home/build/.cargo \
      #       -v $HOME/.m2:/home/build/.m2 \
      #       -v $PWD:/build/ \
      #       -w /build/ \
      #       ansilo-build /build/.github/workflows/scripts/test.sh
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.TESTS_AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.TESTS_AWS_SECRET_ACCESS_KEY }}

      - name: Post tests teardown
        if: always()
        run: |
          docker run \
            -e AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID \
            -e AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY \
            -v $HOME/.cargo:/home/build/.cargo \
            -v $HOME/.m2:/home/build/.m2 \
            -v $PWD:/build/ \
            -w /build/ \
            ansilo-build /build/.github/workflows/scripts/test-teardown.sh
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.TESTS_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TESTS_AWS_SECRET_ACCESS_KEY }}

      - name: Compress caches
        run: |
          .github/workflows/scripts/cache-compress.sh target target.tgz
          .github/workflows/scripts/cache-compress.sh ~/.m2 m2.tgz
          .github/workflows/scripts/cache-compress.sh ~/.cargo cargo.tgz
